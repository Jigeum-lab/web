import {
  DEFAULT_COLORS,
  DEFAULT_FONT_HEIGHT,
  DEFAULT_FONT_SIZES,
} from "./tokens";

export const theme = {
  color: DEFAULT_COLORS,
  fontSizes: DEFAULT_FONT_SIZES,
  lineHeights: DEFAULT_FONT_HEIGHT,
};
